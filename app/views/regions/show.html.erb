<ul>
    <% @regions.each do |region| %>
        <li>
            <%= link_to region.name, region_path(region) %>
            <ol>
                <% region.wineries.each do |winery| %>
                    <li><%= link_to winery.name, winery_path(post) %></li>
                <% end %>
            </ol>
        </li>
    <% end %>
</ul>